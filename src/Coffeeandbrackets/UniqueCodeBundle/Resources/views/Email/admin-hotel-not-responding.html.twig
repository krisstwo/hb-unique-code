<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        {% block title %}Demande de réservation sans réponse{% endblock %}
    </title>
</head>
<body>
<center>
    <table width="100%" border="0" cellpadding="0" cellspacing="0">
        <tr>
            <td align="center" valign="top">
<table width="600px" align="center" border="0" cellpadding="0" cellspacing="0">
    <tr>
        <td>
            <p>
                L'hôtel {{ reservation.getHotel() }} n'a pas répondu à une demande de réservation d'un membre Happybreak. Merci d'appeler l'hôtelier pour lui signaler.
            </p>
            <br>
            <ul>
                <li>
                    {{ customer.getFirstName() }} {{ customer.getLastName() }}
                </li>
                {% if customer.getPhone() %}
                <li>
                    {{ customer.getPhone() }}
                </li>
                {% endif %}
                {% if customer.getEmail() %}
                <li>
                    {{ customer.getEmail() }}
                </li>
                {% endif %}
                <li>
                    {{ reservation.getReservationDate().format('d/m/Y') }} pour {{ reservation.getNumberNight() }} nuit{% if(reservation.getNumberNight() > 1) %}s{% endif %}
                </li>
                <li>
                    {{ reservation.getOffer() }} au tarif de {{ reservation.getOfferPrice()|number_format(2, ',') }} € par nuit.
                </li>
                {% if reservation.getCustomerMsg() %}
                <li>
                    {{ reservation.getCustomerMsg() }}
                </li>
                {% endif %}
            </ul>
        </td>
    </tr>
    <tr>
        <td>
            <p>&nbsp;</p>
            <p>&nbsp;</p>
        </td>
    </tr>
    <tr>
        <td>
            {% include 'UniqueCodeBundle:Email:footer.html.twig' %}
        </td>
    </tr>
</table>
            </td>
        </tr>
    </table>
</center>
{% include 'UniqueCodeBundle:Email:style.html.twig' %}
</body>